

CREATE TABLE `User_Session` (
   `session_id` varchar(36) NOT NULL,
   `user_id` int NOT NULL,
   `device_type` varchar(50) DEFAULT NULL,
   `device_os` varchar(50) DEFAULT NULL,
   `browser` varchar(50) DEFAULT NULL,
   `ip_address` varchar(45) DEFAULT NULL,
   `is_active` tinyint(1) DEFAULT '1',
   `last_activity` datetime DEFAULT NULL,
   `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
   `terminated_at` datetime DEFAULT NULL,
   `expires_at` datetime DEFAULT NULL,
   PRIMARY KEY (`session_id`),
   KEY `fk_user_session` (`user_id`),
   KEY `idx_last_activity` (`last_activity`),
   CONSTRAINT `fk_user_session` FOREIGN KEY (`user_id`) REFERENCES `User` (`user_id`)
 ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci
 
 CREATE TABLE `User` (
   `user_id` int NOT NULL AUTO_INCREMENT,
   `username` varchar(100) NOT NULL,
   `email` varchar(100) NOT NULL,
   `full_name` varchar(100) DEFAULT NULL,
   `password_hash` varchar(255) DEFAULT NULL,
   `password_salt` varchar(255) DEFAULT NULL,
   `role` enum('Super Admin','Admin','Recruiter','Hiring Manager','Interviewer','HR','Employee') DEFAULT NULL,
   `password_last_changed` datetime DEFAULT NULL,
   `password_history` json DEFAULT NULL,
   `mfa_enabled` tinyint(1) DEFAULT '0',
   `mfa_type` enum('SMS','Email','Authenticator') DEFAULT NULL,
   `mfa_secret` varchar(255) DEFAULT NULL,
   `session_timeout` int DEFAULT '3600',
   `last_password_reset` datetime DEFAULT NULL,
   `account_locked` tinyint(1) DEFAULT '0',
   `failed_login_attempts` int DEFAULT '0',
   `security_questions` json DEFAULT NULL,
   `is_active` tinyint(1) DEFAULT '1',
   `last_login` datetime DEFAULT NULL,
   `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
   `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
   PRIMARY KEY (`user_id`),
   UNIQUE KEY `username` (`username`),
   UNIQUE KEY `email` (`email`),
   KEY `idx_user_email` (`email`),
   KEY `idx_user_username` (`username`),
   KEY `idx_user_role` (`role`)
 ) ENGINE=InnoDB AUTO_INCREMENT=43 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci
 
 CREATE TABLE `Sourcing_Channel` (
   `channel_id` int NOT NULL AUTO_INCREMENT,
   `channel_name` varchar(100) DEFAULT NULL,
   `channel_type` enum('Job Board','Social Media','Referral','Agency','Career Site','Event') DEFAULT NULL,
   `cost_model` enum('Pay per Click','Subscription','Pay per Hire','Free') DEFAULT NULL,
   `subscription_details` json DEFAULT NULL,
   `cost_per_hire` decimal(10,2) DEFAULT NULL,
   `effectiveness_score` decimal(3,2) DEFAULT NULL,
   `active_contracts` json DEFAULT NULL,
   `integration_settings` json DEFAULT NULL,
   `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
   `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
   PRIMARY KEY (`channel_id`)
 ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci
 
 CREATE TABLE `Skill_Trend` (
   `trend_id` int NOT NULL AUTO_INCREMENT,
   `skill_id` int DEFAULT NULL,
   `trend_date` date DEFAULT NULL,
   `demand_score` decimal(3,2) DEFAULT NULL,
   `salary_range` json DEFAULT NULL,
   `market_analysis` text,
   `data_source` varchar(100) DEFAULT NULL,
   `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
   PRIMARY KEY (`trend_id`),
   KEY `fk_skill_trend` (`skill_id`),
   CONSTRAINT `fk_skill_trend` FOREIGN KEY (`skill_id`) REFERENCES `Skill` (`skill_id`)
 ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci
 
 CREATE TABLE `Skill_Category` (
   `category_id` int NOT NULL AUTO_INCREMENT,
   `category_name` varchar(100) NOT NULL,
   `description` text,
   `parent_category_id` int DEFAULT NULL,
   `category_level` int DEFAULT NULL,
   `industry_alignment` json DEFAULT NULL,
   `market_demand_score` decimal(3,2) DEFAULT NULL,
   `last_updated_date` datetime DEFAULT NULL,
   `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
   `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
   PRIMARY KEY (`category_id`),
   UNIQUE KEY `category_name` (`category_name`),
   KEY `fk_parent_category` (`parent_category_id`),
   CONSTRAINT `fk_parent_category` FOREIGN KEY (`parent_category_id`) REFERENCES `Skill_Category` (`category_id`)
 ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci
 
 CREATE TABLE `Skill` (
   `skill_id` int NOT NULL AUTO_INCREMENT,
   `category_id` int DEFAULT NULL,
   `skill_name` varchar(100) NOT NULL,
   `description` text,
   `skill_version` varchar(50) DEFAULT NULL,
   `skill_aliases` json DEFAULT NULL,
   `prerequisites` json DEFAULT NULL,
   `assessment_criteria` json DEFAULT NULL,
   `is_technical` tinyint(1) DEFAULT '1',
   `market_relevance_score` decimal(3,2) DEFAULT NULL,
   `trending_score` decimal(3,2) DEFAULT NULL,
   `learning_resources` json DEFAULT NULL,
   `certification_paths` json DEFAULT NULL,
   `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
   `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
   PRIMARY KEY (`skill_id`),
   UNIQUE KEY `skill_name` (`skill_name`),
   KEY `fk_category_skill` (`category_id`),
   CONSTRAINT `fk_category_skill` FOREIGN KEY (`category_id`) REFERENCES `Skill_Category` (`category_id`)
 ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci
 
 CREATE TABLE `saved_reports` (
   `id` bigint unsigned NOT NULL AUTO_INCREMENT,
   `chat_id` varchar(255) DEFAULT NULL,
   `report_name` varchar(255) DEFAULT NULL,
   `content` text,
   `type` varchar(20) DEFAULT NULL,
   `sql_query` text,
   PRIMARY KEY (`id`),
   UNIQUE KEY `id` (`id`)
 ) ENGINE=InnoDB AUTO_INCREMENT=6 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci
 
 CREATE TABLE `Requisition` (
   `requisition_id` int NOT NULL AUTO_INCREMENT,
   `requisition_code` varchar(20) DEFAULT NULL,
   `business_need_id` int DEFAULT NULL,
   `requirement_id` int DEFAULT NULL,
   `budget_id` int DEFAULT NULL,
   `client_id` int DEFAULT NULL,
   `department_id` int DEFAULT NULL,
   `hiring_manager_id` int DEFAULT NULL,
   `recruiter_assigned_id` int DEFAULT NULL,
   `req_priority` enum('Critical','High','Medium','Low') DEFAULT NULL,
   `target_positions` int DEFAULT NULL,
   `filled_positions` int DEFAULT '0',
   `target_start_date` date DEFAULT NULL,
   `target_completion_date` date DEFAULT NULL,
   `time_to_fill_target` int DEFAULT NULL,
   `cost_per_hire_budget` decimal(10,2) DEFAULT NULL,
   `sourcing_strategy` json DEFAULT NULL,
   `diversity_targets` json DEFAULT NULL,
   `recruitment_channels` json DEFAULT NULL,
   `hiring_pipeline_metrics` json DEFAULT NULL,
   `requisition_success_metrics` json DEFAULT NULL,
   `market_analysis_data` json DEFAULT NULL,
   `candidate_pipeline_status` json DEFAULT NULL,
   `internal_notes` text,
   `requisition_status` enum('Draft','Submitted','Approved','On Hold','Closed','Cancelled') DEFAULT NULL,
   `created_by` int DEFAULT NULL,
   `created_date` datetime DEFAULT CURRENT_TIMESTAMP,
   `closing_date` datetime DEFAULT NULL,
   PRIMARY KEY (`requisition_id`),
   UNIQUE KEY `requisition_code` (`requisition_code`),
   KEY `fk_business_needs_requisition` (`business_need_id`),
   KEY `fk_requirement_requisition` (`requirement_id`),
   KEY `fk_budget_requisition` (`budget_id`),
   KEY `fk_client_requisition` (`client_id`),
   KEY `fk_department_requisition` (`department_id`),
   KEY `fk_hiring_manager_requisition` (`hiring_manager_id`),
   KEY `fk_recruiter_requisition` (`recruiter_assigned_id`),
   KEY `fk_created_by_requisition` (`created_by`),
   CONSTRAINT `fk_budget_requisition` FOREIGN KEY (`budget_id`) REFERENCES `Budget_Approval` (`budget_id`),
   CONSTRAINT `fk_business_needs_requisition` FOREIGN KEY (`business_need_id`) REFERENCES `Business_Needs` (`business_need_id`),
   CONSTRAINT `fk_client_requisition` FOREIGN KEY (`client_id`) REFERENCES `Client_Details` (`client_id`),
   CONSTRAINT `fk_created_by_requisition` FOREIGN KEY (`created_by`) REFERENCES `User` (`user_id`),
   CONSTRAINT `fk_department_requisition` FOREIGN KEY (`department_id`) REFERENCES `Department` (`department_id`),
   CONSTRAINT `fk_hiring_manager_requisition` FOREIGN KEY (`hiring_manager_id`) REFERENCES `Employee` (`employee_id`),
   CONSTRAINT `fk_recruiter_requisition` FOREIGN KEY (`recruiter_assigned_id`) REFERENCES `User` (`user_id`),
   CONSTRAINT `fk_requirement_requisition` FOREIGN KEY (`requirement_id`) REFERENCES `Job_Requirements` (`requirement_id`)
 ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci
 
 CREATE TABLE `Requirement_Skills` (
   `requirement_id` int NOT NULL,
   `skill_name` varchar(255) NOT NULL,
   `skill_importance` enum('Must Have','Nice to Have') DEFAULT NULL,
   `skill_experience_years` int DEFAULT NULL,
   `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
   PRIMARY KEY (`requirement_id`,`skill_name`),
   CONSTRAINT `fk_job_req_skill` FOREIGN KEY (`requirement_id`) REFERENCES `Job_Requirements` (`requirement_id`)
 ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci
 
 CREATE TABLE `Onboarding_Schedule` (
   `onboarding_id` int NOT NULL AUTO_INCREMENT,
   `candidate_id` int DEFAULT NULL,
   `employee_id` int DEFAULT NULL,
   `start_date` date DEFAULT NULL,
   `end_date` date DEFAULT NULL,
   `session_type` enum('Orientation','Policy Training','Culture & Values','Other') DEFAULT NULL,
   `session_trainer` varchar(100) DEFAULT NULL,
   `session_location` varchar(100) DEFAULT NULL,
   `completion_status` enum('Completed','On Hold','Pending','Absent','Approval Rejected') DEFAULT NULL,
   `progress_tracking` json DEFAULT NULL,
   `automated_tasks` json DEFAULT NULL,
   `compliance_checklist` json DEFAULT NULL,
   `feedback_scores` json DEFAULT NULL,
   `milestone_completion` json DEFAULT NULL,
   `buddy_assigned_id` int DEFAULT NULL,
   `orientation_materials` json DEFAULT NULL,
   `training_schedule` json DEFAULT NULL,
   `remarks` text,
   `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
   `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
   `isDelete` tinyint(1) DEFAULT '0',
   `onboarding_status` enum('Pending Approval','Approved','Onboarded','Rejected') DEFAULT 'Pending Approval',
   `created_by` int DEFAULT NULL,
   `posting_id` int DEFAULT NULL,
   `requirement_id` int DEFAULT NULL,
   `client_id` int DEFAULT NULL,
   PRIMARY KEY (`onboarding_id`),
   KEY `fk_candidate_onboarding` (`candidate_id`),
   KEY `fk_employee_onboarding` (`employee_id`),
   KEY `fk_buddy_onboarding` (`buddy_assigned_id`),
   KEY `fk_created_by_onboarding` (`created_by`),
   CONSTRAINT `fk_buddy_onboarding` FOREIGN KEY (`buddy_assigned_id`) REFERENCES `Employee` (`employee_id`),
   CONSTRAINT `fk_candidate_onboarding` FOREIGN KEY (`candidate_id`) REFERENCES `Candidate_Profile` (`candidate_id`),
   CONSTRAINT `fk_created_by_onboarding` FOREIGN KEY (`created_by`) REFERENCES `User` (`user_id`),
   CONSTRAINT `fk_employee_onboarding` FOREIGN KEY (`employee_id`) REFERENCES `Employee` (`employee_id`)
 ) ENGINE=InnoDB AUTO_INCREMENT=5 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci
 
 CREATE TABLE `offer_templates` (
   `template_id` int NOT NULL AUTO_INCREMENT,
   `template_name` varchar(255) CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci NOT NULL,
   `template_description` text CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci,
   `template_content` longtext CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci,
   `placeholders` json DEFAULT NULL,
   `created_by` int DEFAULT NULL,
   `created_at` timestamp NULL DEFAULT CURRENT_TIMESTAMP,
   `updated_at` timestamp NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
   PRIMARY KEY (`template_id`),
   KEY `fk_user_template` (`created_by`),
   CONSTRAINT `fk_user_template` FOREIGN KEY (`created_by`) REFERENCES `User` (`user_id`)
 ) ENGINE=InnoDB AUTO_INCREMENT=12 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
 
 CREATE TABLE `Offer_Details` (
   `offer_id` int NOT NULL AUTO_INCREMENT,
   `candidate_id` int DEFAULT NULL,
   `requisition_id` int DEFAULT NULL,
   `job_title` varchar(100) DEFAULT NULL,
   `department_id` int DEFAULT NULL,
   `reporting_to` int DEFAULT NULL,
   `offer_date` date DEFAULT NULL,
   `proposed_start_date` date DEFAULT NULL,
   `expiry_date` date DEFAULT NULL,
   `base_salary` decimal(15,2) DEFAULT NULL,
   `variable_pay` decimal(15,2) DEFAULT NULL,
   `bonus_details` json DEFAULT NULL,
   `stock_options` json DEFAULT NULL,
   `benefits_package` json DEFAULT NULL,
   `compensation_package_builder` json DEFAULT NULL,
   `market_rate_comparison` json DEFAULT NULL,
   `internal_equity_analysis` json DEFAULT NULL,
   `acceptance_probability` decimal(3,2) DEFAULT NULL,
   `total_compensation_value` decimal(15,2) DEFAULT NULL,
   `approval_workflow_status` json DEFAULT NULL,
   `offer_letter_variables` json DEFAULT NULL,
   `negotiation_history` json DEFAULT NULL,
   `offer_status` enum('Draft','Pending Approval','Pending Rejected','Approved','Extended','Accepted','Declined','Expired','Revoke') DEFAULT NULL,
   `created_by` int DEFAULT NULL,
   `isDelete` tinyint(1) DEFAULT '0',
   `created_date` datetime DEFAULT CURRENT_TIMESTAMP,
   `updated_date` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
   `posting_id` int DEFAULT NULL,
   `requirement_id` int DEFAULT NULL,
   `client_id` int DEFAULT NULL,
   PRIMARY KEY (`offer_id`),
   KEY `fk_candidate_offer` (`candidate_id`),
   KEY `fk_department_offer` (`department_id`),
   KEY `fk_reporting_offer` (`reporting_to`),
   KEY `fk_user_offer` (`created_by`),
   CONSTRAINT `fk_candidate_offer` FOREIGN KEY (`candidate_id`) REFERENCES `Candidate_Profile` (`candidate_id`),
   CONSTRAINT `fk_department_offer` FOREIGN KEY (`department_id`) REFERENCES `Department` (`department_id`),
   CONSTRAINT `fk_reporting_offer` FOREIGN KEY (`reporting_to`) REFERENCES `Employee` (`employee_id`),
   CONSTRAINT `fk_user_offer` FOREIGN KEY (`created_by`) REFERENCES `User` (`user_id`)
 ) ENGINE=InnoDB AUTO_INCREMENT=7 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci
 
 CREATE TABLE `Offer_Analytics` (
   `analytics_id` int NOT NULL AUTO_INCREMENT,
   `offer_id` int DEFAULT NULL,
   `market_percentile` decimal(5,2) DEFAULT NULL,
   `industry_comparison` json DEFAULT NULL,
   `peer_comparison` json DEFAULT NULL,
   `budget_variance` decimal(15,2) DEFAULT NULL,
   `risk_assessment` json DEFAULT NULL,
   `acceptance_factors` json DEFAULT NULL,
   `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
   PRIMARY KEY (`analytics_id`)
 ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci
 
 CREATE TABLE `Negotiation_History` (
   `negotiation_id` int NOT NULL AUTO_INCREMENT,
   `offer_id` int DEFAULT NULL,
   `candidate_id` int DEFAULT NULL,
   `proposed_salary` decimal(10,2) DEFAULT NULL,
   `proposed_benefits` text CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci,
   `discussion_notes` text CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci,
   `negotiation_status` enum('In Progress','Accepted','Rejected') CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci DEFAULT NULL,
   `negotiation_date` datetime DEFAULT CURRENT_TIMESTAMP,
   `negotiated_by` int DEFAULT NULL,
   `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
   PRIMARY KEY (`negotiation_id`),
   KEY `fk_offer_negotiation` (`offer_id`),
   KEY `fk_candidate_negotiation` (`candidate_id`),
   KEY `fk_user_negotiation` (`negotiated_by`),
   CONSTRAINT `fk_candidate_negotiation` FOREIGN KEY (`candidate_id`) REFERENCES `Candidate_Profile` (`candidate_id`),
   CONSTRAINT `fk_offer_negotiation` FOREIGN KEY (`offer_id`) REFERENCES `Offer_Details` (`offer_id`),
   CONSTRAINT `fk_user_negotiation` FOREIGN KEY (`negotiated_by`) REFERENCES `User` (`user_id`)
 ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
 
 CREATE TABLE `Location` (
   `location_id` int NOT NULL AUTO_INCREMENT,
   `company_id` int NOT NULL,
   `location_name` varchar(100) NOT NULL,
   `location_type` enum('Office','Remote','Hybrid') DEFAULT NULL,
   `address` text,
   `city` varchar(100) DEFAULT NULL,
   `state` varchar(100) DEFAULT NULL,
   `country` varchar(100) DEFAULT NULL,
   `postal_code` varchar(20) DEFAULT NULL,
   `timezone` varchar(50) DEFAULT NULL,
   `capacity` int DEFAULT NULL,
   `facility_manager_id` int DEFAULT NULL,
   `operating_hours` json DEFAULT NULL,
   `amenities` json DEFAULT NULL,
   `safety_compliance` json DEFAULT NULL,
   `last_safety_audit` date DEFAULT NULL,
   `covid_safety_level` enum('Level1','Level2','Level3','Level4') DEFAULT NULL,
   `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
   `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
   `isDelete` tinyint DEFAULT '0',
   PRIMARY KEY (`location_id`),
   KEY `fk_company_location` (`company_id`),
   CONSTRAINT `fk_company_location` FOREIGN KEY (`company_id`) REFERENCES `Company` (`company_id`)
 ) ENGINE=InnoDB AUTO_INCREMENT=9 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci
 
 CREATE TABLE `Job_Requirements` (
   `requirement_id` int NOT NULL AUTO_INCREMENT,
   `business_need_id` int DEFAULT NULL,
   `job_title` varchar(100) NOT NULL,
   `job_code` varchar(50) DEFAULT NULL,
   `job_description` text,
   `job_responsibilities` text,
   `minimum_experience` int DEFAULT NULL,
   `maximum_experience` int DEFAULT NULL,
   `education_qualification` varchar(255) DEFAULT NULL,
   `certification_requirements` text,
   `work_location_id` int DEFAULT NULL,
   `remote_work_option` tinyint(1) DEFAULT '0',
   `travel_requirement` enum('None','Minimal','Moderate','Extensive') DEFAULT NULL,
   `shift_timing` enum('Regular','Night','Rotational','Flexible') DEFAULT NULL,
   `work_schedule` enum('Full-Time','Part-Time','Flexible','Contract') DEFAULT NULL,
   `salary_band_min` decimal(15,2) DEFAULT NULL,
   `salary_band_max` decimal(15,2) DEFAULT NULL,
   `bonus_structure` json DEFAULT NULL,
   `benefits_package` json DEFAULT NULL,
   `success_criteria` json DEFAULT NULL,
   `performance_indicators` json DEFAULT NULL,
   `competency_framework` json DEFAULT NULL,
   `career_progression_path` json DEFAULT NULL,
   `market_salary_data` json DEFAULT NULL,
   `internal_equity_analysis` json DEFAULT NULL,
   `created_by` int DEFAULT NULL,
   `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
   `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
   `status` enum('Draft','Finalized','Approved','Archived') DEFAULT NULL,
   `isDelete` tinyint(1) DEFAULT '0',
   `project_id` int DEFAULT NULL,
   PRIMARY KEY (`requirement_id`),
   KEY `fk_business_needs_job` (`business_need_id`),
   KEY `fk_location_job` (`work_location_id`),
   KEY `fk_created_by_job` (`created_by`),
   CONSTRAINT `fk_business_needs_job` FOREIGN KEY (`business_need_id`) REFERENCES `Business_Needs` (`business_need_id`),
   CONSTRAINT `fk_created_by_job` FOREIGN KEY (`created_by`) REFERENCES `User` (`user_id`)
 ) ENGINE=InnoDB AUTO_INCREMENT=15 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci
 
 CREATE TABLE `Job_Posting` (
   `posting_id` int NOT NULL AUTO_INCREMENT,
   `requirement_id` int DEFAULT NULL,
   `client_id` int DEFAULT NULL,
   `posting_title` varchar(255) DEFAULT NULL,
   `posting_description` text,
   `posting_status` enum('Draft','Published','Pending Approval','Closed','On Hold') DEFAULT NULL,
   `visibility` enum('Public','Private','Internal') DEFAULT NULL,
   `external_posting_urls` json DEFAULT NULL,
   `application_deadline` date DEFAULT NULL,
   `created_by` int DEFAULT NULL,
   `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
   `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
   `isDelete` tinyint(1) DEFAULT '0',
   `isApproval` enum('Pending Approval','approved','Rejected') DEFAULT 'Pending Approval',
   PRIMARY KEY (`posting_id`),
   KEY `fk_requirement_posting` (`requirement_id`),
   KEY `fk_created_by_posting` (`created_by`),
   KEY `fk_client_id_posting` (`client_id`),
   CONSTRAINT `fk_client_id_posting` FOREIGN KEY (`client_id`) REFERENCES `Client_Details` (`client_id`),
   CONSTRAINT `fk_created_by_posting` FOREIGN KEY (`created_by`) REFERENCES `User` (`user_id`),
   CONSTRAINT `fk_requirement_posting` FOREIGN KEY (`requirement_id`) REFERENCES `Job_Requirements` (`requirement_id`)
 ) ENGINE=InnoDB AUTO_INCREMENT=15 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci
 
 CREATE TABLE `IT_Asset_Management` (
   `asset_id` int NOT NULL AUTO_INCREMENT,
   `candidate_id` int DEFAULT NULL,
   `laptop_serial_number` varchar(100) DEFAULT NULL,
   `email_account_status` enum('Created','Pending','Not Required') DEFAULT NULL,
   `software_tools_provided` text,
   `access_card_issued` tinyint(1) DEFAULT NULL,
   `asset_tracking_number` varchar(100) DEFAULT NULL,
   `asset_history` json DEFAULT NULL,
   `software_licenses` json DEFAULT NULL,
   `access_levels` json DEFAULT NULL,
   `security_clearance_status` varchar(50) DEFAULT NULL,
   `equipment_cost` decimal(10,2) DEFAULT NULL,
   `maintenance_schedule` json DEFAULT NULL,
   `delivery_status` enum('Delivered','Pending','Not Required') DEFAULT NULL,
   `delivery_date` datetime DEFAULT NULL,
   `return_date` date DEFAULT NULL,
   `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
   `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
   PRIMARY KEY (`asset_id`),
   KEY `fk_candidate_it` (`candidate_id`),
   CONSTRAINT `fk_candidate_it` FOREIGN KEY (`candidate_id`) REFERENCES `Candidate_Profile` (`candidate_id`)
 ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci
 
 CREATE TABLE `Interviewer_Profile` (
   `interviewer_id` int NOT NULL AUTO_INCREMENT,
   `user_id` int DEFAULT NULL,
   `interviewer_name` varchar(100) DEFAULT NULL,
   `interviewer_email` varchar(100) DEFAULT NULL,
   `interviewer_type` enum('Internal','Client-side','External') DEFAULT NULL,
   `organization_name` varchar(100) DEFAULT NULL,
   `designation` varchar(100) DEFAULT NULL,
   `expertise_area` text,
   `phone_number` varchar(20) DEFAULT NULL,
   `availability_calendar` json DEFAULT NULL,
   `interview_preferences` json DEFAULT NULL,
   `max_interviews_per_day` int DEFAULT NULL,
   `rating` decimal(3,2) DEFAULT NULL,
   `status` enum('Active','Inactive','OnLeave') DEFAULT NULL,
   `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
   `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
   `is_delete` tinyint DEFAULT '0',
   PRIMARY KEY (`interviewer_id`),
   KEY `fk_user_interviewer` (`user_id`),
   CONSTRAINT `fk_user_interviewer` FOREIGN KEY (`user_id`) REFERENCES `User` (`user_id`)
 ) ENGINE=InnoDB AUTO_INCREMENT=8 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci
 
 CREATE TABLE `Interview_Schedule` (
   `interview_id` int NOT NULL AUTO_INCREMENT,
   `candidate_id` int DEFAULT NULL,
   `requisition_id` int DEFAULT NULL,
   `posting_id` int DEFAULT NULL,
   `requirement_id` int DEFAULT NULL,
   `client_id` int DEFAULT NULL,
   `interviewer_id` int DEFAULT NULL,
   `interview_round_id` int DEFAULT NULL,
   `interview_date` datetime DEFAULT NULL,
   `end_time` datetime DEFAULT NULL,
   `interview_mode` enum('On-site','Virtual','Telephonic') DEFAULT NULL,
   `meeting_link` text,
   `location_details` varchar(255) DEFAULT NULL,
   `status` enum('Draft','Scheduled','Completed','Pending_Schedule','Pending_Reschedule','Rescheduled','Cancelled','No Show') DEFAULT NULL,
   `calendar_event_id` varchar(255) DEFAULT NULL,
   `interviewer_availability` json DEFAULT NULL,
   `interview_preparation_sent` tinyint(1) DEFAULT NULL,
   `interview_cost` decimal(10,2) DEFAULT NULL,
   `interview_questions` json DEFAULT NULL,
   `ai_evaluation_metrics` json DEFAULT NULL,
   `recording_consent` tinyint(1) DEFAULT NULL,
   `interview_guidelines` text,
   `technical_requirements` json DEFAULT NULL,
   `created_by` int DEFAULT NULL,
   `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
   `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
   `is_approval` enum('Pending Approval','Approved','Rejected') DEFAULT 'Pending Approval',
   PRIMARY KEY (`interview_id`),
   KEY `fk_candidate_interview` (`candidate_id`),
   KEY `fk_posting_interview` (`posting_id`),
   KEY `fk_interviewer_interview` (`interviewer_id`),
   KEY `fk_round_interview` (`interview_round_id`),
   KEY `fk_user_interview` (`created_by`),
   KEY `fk_requirement_interview` (`requirement_id`),
   KEY `fk_client_interview` (`client_id`),
   CONSTRAINT `fk_candidate_interview` FOREIGN KEY (`candidate_id`) REFERENCES `Candidate_Profile` (`candidate_id`),
   CONSTRAINT `fk_client_interview` FOREIGN KEY (`client_id`) REFERENCES `Client_Details` (`client_id`) ON DELETE CASCADE ON UPDATE CASCADE,
   CONSTRAINT `fk_interviewer_interview` FOREIGN KEY (`interviewer_id`) REFERENCES `Interviewer_Profile` (`interviewer_id`),
   CONSTRAINT `fk_posting_interview` FOREIGN KEY (`posting_id`) REFERENCES `Job_Posting` (`posting_id`),
   CONSTRAINT `fk_requirement_interview` FOREIGN KEY (`requirement_id`) REFERENCES `Job_Requirements` (`requirement_id`) ON DELETE CASCADE ON UPDATE CASCADE,
   CONSTRAINT `fk_round_interview` FOREIGN KEY (`interview_round_id`) REFERENCES `Interview_Round` (`interview_round_id`),
   CONSTRAINT `fk_user_interview` FOREIGN KEY (`created_by`) REFERENCES `User` (`user_id`)
 ) ENGINE=InnoDB AUTO_INCREMENT=31 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci
 
 CREATE TABLE `Interview_Round` (
   `interview_round_id` int NOT NULL AUTO_INCREMENT,
   `round_name` enum('Technical','Behavioral','Panel','Situational','Case Study','HR','Final','Ready to Offer') DEFAULT NULL,
   `round_description` text,
   `duration_minutes` int DEFAULT NULL,
   `evaluation_criteria` text,
   `scoring_weightage` decimal(5,2) DEFAULT NULL,
   `is_mandatory` tinyint(1) DEFAULT NULL,
   `question_template_id` int DEFAULT NULL,
   `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
   `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
   PRIMARY KEY (`interview_round_id`)
 ) ENGINE=InnoDB AUTO_INCREMENT=9 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci
 
 CREATE TABLE `Interview_Feedback` (
   `feedback_id` int NOT NULL AUTO_INCREMENT,
   `interview_id` int DEFAULT NULL,
   `interviewer_id` int DEFAULT NULL,
   `round_feedback` text,
   `technical_score` decimal(5,2) DEFAULT NULL,
   `communication_score` decimal(5,2) DEFAULT NULL,
   `cultural_fit_score` decimal(5,2) DEFAULT NULL,
   `structured_feedback` json DEFAULT NULL,
   `skill_assessment` json DEFAULT NULL,
   `cultural_fit_assessment` json DEFAULT NULL,
   `red_flags` text,
   `growth_potential` decimal(3,2) DEFAULT NULL,
   `ai_analysis_results` json DEFAULT NULL,
   `interview_recording_url` text,
   `feedback_quality_score` decimal(3,2) DEFAULT NULL,
   `feedback_date` datetime DEFAULT CURRENT_TIMESTAMP,
   `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
   `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
   `final_recommendation` enum('Selected','Rejected') DEFAULT NULL,
   PRIMARY KEY (`feedback_id`),
   KEY `fk_interview_feedback` (`interview_id`),
   KEY `fk_interviewer_feedback` (`interviewer_id`),
   CONSTRAINT `fk_interview_feedback` FOREIGN KEY (`interview_id`) REFERENCES `Interview_Schedule` (`interview_id`),
   CONSTRAINT `fk_interviewer_feedback` FOREIGN KEY (`interviewer_id`) REFERENCES `Interviewer_Profile` (`interviewer_id`)
 ) ENGINE=InnoDB AUTO_INCREMENT=14 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci
 
 CREATE TABLE `Employee` (
   `employee_id` int NOT NULL AUTO_INCREMENT,
   `company_id` int NOT NULL,
   `department_id` int DEFAULT NULL,
   `location_id` int DEFAULT NULL,
   `user_id` int DEFAULT NULL,
   `first_name` varchar(100) NOT NULL,
   `last_name` varchar(100) NOT NULL,
   `email` varchar(100) NOT NULL,
   `phone` varchar(20) DEFAULT NULL,
   `designation` varchar(100) DEFAULT NULL,
   `employee_type` enum('Full-time','Part-time','Contractor','Intern') DEFAULT NULL,
   `reports_to` int DEFAULT NULL,
   `hire_date` date DEFAULT NULL,
   `termination_date` date DEFAULT NULL,
   `emergency_contact` json DEFAULT NULL,
   `work_schedule` json DEFAULT NULL,
   `skills_matrix` json DEFAULT NULL,
   `certifications` json DEFAULT NULL,
   `performance_rating` decimal(3,2) DEFAULT NULL,
   `last_review_date` date DEFAULT NULL,
   `next_review_date` date DEFAULT NULL,
   `career_path` json DEFAULT NULL,
   `training_completed` json DEFAULT NULL,
   `compliance_training_status` json DEFAULT NULL,
   `is_active` tinyint(1) DEFAULT '1',
   `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
   `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
   PRIMARY KEY (`employee_id`),
   UNIQUE KEY `email` (`email`),
   KEY `fk_company_employee` (`company_id`),
   KEY `fk_department_employee` (`department_id`),
   KEY `fk_location_employee` (`location_id`),
   KEY `fk_user_employee` (`user_id`),
   KEY `fk_reports_to` (`reports_to`),
   CONSTRAINT `fk_company_employee` FOREIGN KEY (`company_id`) REFERENCES `Company` (`company_id`),
   CONSTRAINT `fk_department_employee` FOREIGN KEY (`department_id`) REFERENCES `Department` (`department_id`),
   CONSTRAINT `fk_location_employee` FOREIGN KEY (`location_id`) REFERENCES `Location` (`location_id`),
   CONSTRAINT `fk_reports_to` FOREIGN KEY (`reports_to`) REFERENCES `Employee` (`employee_id`),
   CONSTRAINT `fk_user_employee` FOREIGN KEY (`user_id`) REFERENCES `User` (`user_id`)
 ) ENGINE=InnoDB AUTO_INCREMENT=13 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci
 
 CREATE TABLE `Department` (
   `department_id` int NOT NULL AUTO_INCREMENT,
   `company_id` int NOT NULL,
   `department_name` varchar(100) NOT NULL,
   `department_head_id` int DEFAULT NULL,
   `parent_department_id` int DEFAULT NULL,
   `cost_center_code` varchar(50) DEFAULT NULL,
   `department_type` enum('Core','Support','Administrative','Research') DEFAULT NULL,
   `headcount_limit` int DEFAULT NULL,
   `current_headcount` int DEFAULT NULL,
   `annual_budget` decimal(15,2) DEFAULT NULL,
   `operational_metrics` json DEFAULT NULL,
   `department_kpis` json DEFAULT NULL,
   `budget` decimal(15,2) DEFAULT NULL,
   `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
   `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
   `isDelete` tinyint(1) DEFAULT '0',
   PRIMARY KEY (`department_id`),
   KEY `fk_company_department` (`company_id`),
   KEY `fk_parent_department` (`parent_department_id`),
   CONSTRAINT `fk_company_department` FOREIGN KEY (`company_id`) REFERENCES `Company` (`company_id`),
   CONSTRAINT `fk_parent_department` FOREIGN KEY (`parent_department_id`) REFERENCES `Department` (`department_id`)
 ) ENGINE=InnoDB AUTO_INCREMENT=16 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci
 
 CREATE TABLE `Company` (
   `company_id` int NOT NULL AUTO_INCREMENT,
   `company_name` varchar(100) NOT NULL,
   `company_description` text,
   `industry` varchar(100) DEFAULT NULL,
   `company_size` varchar(50) DEFAULT NULL,
   `website` varchar(255) DEFAULT NULL,
   `logo_path` varchar(255) DEFAULT NULL,
   `parent_company_id` int DEFAULT NULL,
   `fiscal_year_start` date DEFAULT NULL,
   `primary_timezone` varchar(50) DEFAULT NULL,
   `tax_id` varchar(50) DEFAULT NULL,
   `compliance_status` enum('Compliant','Under Review','Non-Compliant') DEFAULT NULL,
   `data_protection_officer_id` int DEFAULT NULL,
   `operational_hours` json DEFAULT NULL,
   `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
   `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
   PRIMARY KEY (`company_id`),
   KEY `fk_parent_company` (`parent_company_id`),
   CONSTRAINT `fk_parent_company` FOREIGN KEY (`parent_company_id`) REFERENCES `Company` (`company_id`)
 ) ENGINE=InnoDB AUTO_INCREMENT=5 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci
 
 CREATE TABLE `Client_Details` (
   `client_id` int NOT NULL AUTO_INCREMENT,
   `company_id` int NOT NULL,
   `client_name` varchar(100) NOT NULL,
   `industry_sector` varchar(100) DEFAULT NULL,
   `website` varchar(255) DEFAULT NULL,
   `address` text,
   `city` varchar(100) DEFAULT NULL,
   `state` varchar(100) DEFAULT NULL,
   `country` varchar(100) DEFAULT NULL,
   `postal_code` varchar(20) DEFAULT NULL,
   `account_manager_id` int DEFAULT NULL,
   `client_rating` decimal(3,2) DEFAULT NULL,
   `engagement_history` json DEFAULT NULL,
   `revenue_contribution` decimal(15,2) DEFAULT NULL,
   `satisfaction_score` decimal(3,2) DEFAULT NULL,
   `last_review_date` date DEFAULT NULL,
   `risk_assessment` json DEFAULT NULL,
   `communication_preferences` json DEFAULT NULL,
   `status` enum('Active','Inactive','Prospect') DEFAULT NULL,
   `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
   `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
   `isDelete` tinyint(1) DEFAULT '0',
   `project_id` int DEFAULT NULL,
   `project_name` varchar(255) DEFAULT NULL,
   `account_manager_name` varchar(45) DEFAULT NULL,
   PRIMARY KEY (`client_id`),
   KEY `fk_account_manager` (`account_manager_id`),
   KEY `fk_company_client` (`company_id`),
   CONSTRAINT `fk_account_manager` FOREIGN KEY (`account_manager_id`) REFERENCES `Employee` (`employee_id`),
   CONSTRAINT `fk_company_client` FOREIGN KEY (`company_id`) REFERENCES `Company` (`company_id`)
 ) ENGINE=InnoDB AUTO_INCREMENT=15 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci
 
 CREATE TABLE `Client_Contact` (
   `contact_id` int NOT NULL AUTO_INCREMENT,
   `client_id` int NOT NULL,
   `first_name` varchar(100) NOT NULL,
   `last_name` varchar(100) NOT NULL,
   `designation` varchar(100) DEFAULT NULL,
   `email` varchar(100) NOT NULL,
   `phone` varchar(20) DEFAULT NULL,
   `is_primary` tinyint(1) DEFAULT '0',
   `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
   `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
   `isDelete` tinyint(1) DEFAULT '0',
   PRIMARY KEY (`contact_id`),
   KEY `fk_client_contact` (`client_id`),
   CONSTRAINT `fk_client_contact` FOREIGN KEY (`client_id`) REFERENCES `Client_Details` (`client_id`)
 ) ENGINE=InnoDB AUTO_INCREMENT=5 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci
 
 CREATE TABLE `Candidate_Skills` (
   `candidate_id` int NOT NULL,
   `skill_id` int DEFAULT NULL,
   `skill_title` varchar(255) NOT NULL,
   `skill_details` json DEFAULT NULL,
   `skill_level` enum('Beginner','Intermediate','Advanced','Expert') DEFAULT NULL,
   `years_of_experience` decimal(4,2) DEFAULT NULL,
   `last_used_date` date DEFAULT NULL,
   `is_highlighted` tinyint(1) DEFAULT '0',
   `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
   `isDelete` tinyint(1) DEFAULT '0',
   PRIMARY KEY (`candidate_id`,`skill_title`),
   CONSTRAINT `fk_candidate_skill` FOREIGN KEY (`candidate_id`) REFERENCES `Candidate_Profile` (`candidate_id`)
 ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci
 
 CREATE TABLE `Candidate_Profile` (
   `candidate_id` int NOT NULL AUTO_INCREMENT,
   `first_name` varchar(100) NOT NULL,
   `last_name` varchar(100) NOT NULL,
   `email` varchar(100) NOT NULL,
   `phone` varchar(20) DEFAULT NULL,
   `alternate_phone` varchar(20) DEFAULT NULL,
   `date_of_birth` date DEFAULT NULL,
   `gender` enum('Male','Female','Non-Binary','Prefer not to say') DEFAULT NULL,
   `address` text,
   `city` varchar(100) DEFAULT NULL,
   `state` varchar(100) DEFAULT NULL,
   `country` varchar(100) DEFAULT NULL,
   `postal_code` varchar(20) DEFAULT NULL,
   `nationality` varchar(100) DEFAULT NULL,
   `immigration_status` varchar(100) DEFAULT NULL,
   `social_media_links` json DEFAULT NULL,
   `current_employer` varchar(100) DEFAULT NULL,
   `current_designation` varchar(100) DEFAULT NULL,
   `current_location` varchar(100) DEFAULT NULL,
   `experience_years` decimal(4,2) DEFAULT NULL,
   `current_salary` decimal(15,2) DEFAULT NULL,
   `current_salary_currency` varchar(255) DEFAULT NULL,
   `expected_salary` decimal(15,2) DEFAULT NULL,
   `notice_period` varchar(50) DEFAULT NULL,
   `availability_status` enum('Immediately Available','Serving Notice','Available After Notice','Passive') DEFAULT NULL,
   `willing_to_relocate` tinyint(1) DEFAULT NULL,
   `preferred_locations` json DEFAULT NULL,
   `resume_file_path` text,
   `profile_picture_path` text,
   `source_channel_id` int DEFAULT NULL,
   `source_details` text,
   `tags` json DEFAULT NULL,
   `profile_completeness` int DEFAULT '0',
   `profile_visibility` enum('Public','Private','Limited') DEFAULT NULL,
   `profile_status` enum('Active','Inactive','Pending Approval','Blacklisted','Mapped') DEFAULT 'Active',
   `gdpr_consent` tinyint(1) DEFAULT '0',
   `gdpr_consent_date` datetime DEFAULT NULL,
   `data_retention_date` date DEFAULT NULL,
   `candidate_preferences` json DEFAULT NULL,
   `automated_score` decimal(5,2) DEFAULT NULL,
   `talent_pool_category` varchar(50) DEFAULT NULL,
   `engagement_index` decimal(3,2) DEFAULT NULL,
   `communication_preferences` json DEFAULT NULL,
   `candidate_journey_status` json DEFAULT NULL,
   `assessment_results` json DEFAULT NULL,
   `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
   `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
   `isDelete` tinyint(1) DEFAULT '0',
   `is_approval` enum('Pending Approval','Approved') DEFAULT 'Pending Approval',
   `incharge_by` int DEFAULT NULL,
   PRIMARY KEY (`candidate_id`),
   UNIQUE KEY `email` (`email`),
   KEY `fk_candidate_incharge_by` (`incharge_by`),
   CONSTRAINT `fk_candidate_incharge_by` FOREIGN KEY (`incharge_by`) REFERENCES `User` (`user_id`)
 ) ENGINE=InnoDB AUTO_INCREMENT=50 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci
 
 CREATE TABLE `Candidate_Experience` (
   `experience_id` int NOT NULL AUTO_INCREMENT,
   `candidate_id` int DEFAULT NULL,
   `company_name` varchar(255) NOT NULL,
   `designation` varchar(255) NOT NULL,
   `start_date` date NOT NULL,
   `end_date` date DEFAULT NULL,
   `is_current` tinyint(1) DEFAULT '0',
   `job_description` text,
   `achievements` text,
   `location` varchar(100) DEFAULT NULL,
   `employment_type` enum('Full-time','Part-time','Contract','Freelance','Internship') DEFAULT NULL,
   `is_verified` tinyint(1) DEFAULT '0',
   `verification_date` datetime DEFAULT NULL,
   `verification_notes` text,
   `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
   `isDelete` tinyint(1) DEFAULT '0',
   PRIMARY KEY (`experience_id`),
   KEY `fk_candidate_exp` (`candidate_id`),
   CONSTRAINT `fk_candidate_exp` FOREIGN KEY (`candidate_id`) REFERENCES `Candidate_Profile` (`candidate_id`)
 ) ENGINE=InnoDB AUTO_INCREMENT=114 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci
 
 CREATE TABLE `Candidate_Education` (
   `education_id` int NOT NULL AUTO_INCREMENT,
   `candidate_id` int DEFAULT NULL,
   `institution_name` varchar(255) NOT NULL,
   `degree` varchar(255) NOT NULL,
   `field_of_study` varchar(255) DEFAULT NULL,
   `start_date` date DEFAULT NULL,
   `end_date` date DEFAULT NULL,
   `grade` varchar(50) DEFAULT NULL,
   `description` text,
   `is_verified` tinyint(1) DEFAULT '0',
   `verification_date` datetime DEFAULT NULL,
   `verification_notes` text,
   `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
   `isDelete` tinyint(1) DEFAULT '0',
   PRIMARY KEY (`education_id`),
   KEY `fk_candidate_edu` (`candidate_id`),
   CONSTRAINT `fk_candidate_edu` FOREIGN KEY (`candidate_id`) REFERENCES `Candidate_Profile` (`candidate_id`)
 ) ENGINE=InnoDB AUTO_INCREMENT=82 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci
 
 CREATE TABLE `Candidate_Document` (
   `document_id` int NOT NULL AUTO_INCREMENT,
   `candidate_id` int DEFAULT NULL,
   `document_type` enum('Resume','Cover Letter','ID Proof','Educational Certificate','Experience Certificate','Reference Letter','Other') DEFAULT NULL,
   `document_title` varchar(255) DEFAULT NULL,
   `document_path` text NOT NULL,
   `upload_date` datetime DEFAULT CURRENT_TIMESTAMP,
   `is_verified` tinyint(1) DEFAULT '0',
   `verification_date` datetime DEFAULT NULL,
   `verification_by` int DEFAULT NULL,
   `notes` text,
   `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
   PRIMARY KEY (`document_id`),
   KEY `fk_candidate_doc` (`candidate_id`),
   KEY `fk_user_doc` (`verification_by`),
   CONSTRAINT `fk_candidate_doc` FOREIGN KEY (`candidate_id`) REFERENCES `Candidate_Profile` (`candidate_id`),
   CONSTRAINT `fk_user_doc` FOREIGN KEY (`verification_by`) REFERENCES `User` (`user_id`)
 ) ENGINE=InnoDB AUTO_INCREMENT=2 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci
 
 CREATE TABLE `Candidate_Certification` (
   `certification_id` int NOT NULL AUTO_INCREMENT,
   `candidate_id` int DEFAULT NULL,
   `certification_name` varchar(255) NOT NULL,
   `issuing_organization` varchar(255) DEFAULT NULL,
   `issue_date` date DEFAULT NULL,
   `expiry_date` date DEFAULT NULL,
   `credential_id` varchar(100) DEFAULT NULL,
   `credential_url` varchar(255) DEFAULT NULL,
   `is_verified` tinyint(1) DEFAULT '0',
   `verification_date` datetime DEFAULT NULL,
   `verification_notes` text,
   `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
   PRIMARY KEY (`certification_id`),
   KEY `fk_candidate_cert` (`candidate_id`),
   CONSTRAINT `fk_candidate_cert` FOREIGN KEY (`candidate_id`) REFERENCES `Candidate_Profile` (`candidate_id`)
 ) ENGINE=InnoDB AUTO_INCREMENT=41 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci
 
 CREATE TABLE `Candidate_Application` (
   `application_id` int NOT NULL AUTO_INCREMENT,
   `candidate_id` int DEFAULT NULL,
   `requisition_id` int DEFAULT NULL,
   `posting_id` int DEFAULT NULL,
   `requirement_id` int DEFAULT NULL,
   `client_id` int DEFAULT NULL,
   `application_date` datetime DEFAULT CURRENT_TIMESTAMP,
   `cover_letter` text,
   `status` varchar(50) DEFAULT NULL,
   `current_stage` varchar(50) DEFAULT NULL,
   `stage_history` json DEFAULT NULL,
   `assessment_scores` json DEFAULT NULL,
   `interview_feedback_summary` json DEFAULT NULL,
   `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
   `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
   `incharge_by` int DEFAULT NULL,
   `created_by` int DEFAULT NULL,
   `matching_score` int DEFAULT NULL,
   `reason` json DEFAULT NULL,
   PRIMARY KEY (`application_id`),
   KEY `fk_created_by_candidate` (`created_by`),
   KEY `fk_requirement_app` (`requirement_id`),
   KEY `fk_client_app` (`client_id`),
   KEY `fk_incharge_by` (`incharge_by`),
   CONSTRAINT `fk_client_app` FOREIGN KEY (`client_id`) REFERENCES `Client_Details` (`client_id`),
   CONSTRAINT `fk_created_by_candidate` FOREIGN KEY (`created_by`) REFERENCES `User` (`user_id`),
   CONSTRAINT `fk_incharge_by` FOREIGN KEY (`incharge_by`) REFERENCES `User` (`user_id`),
   CONSTRAINT `fk_requirement_app` FOREIGN KEY (`requirement_id`) REFERENCES `Job_Requirements` (`requirement_id`)
 ) ENGINE=InnoDB AUTO_INCREMENT=47 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci
 
 CREATE TABLE `Business_Needs` (
   `business_need_id` int NOT NULL AUTO_INCREMENT,
   `client_id` int DEFAULT NULL,
   `department_id` int DEFAULT NULL,
   `business_objective` text,
   `project_name` varchar(100) DEFAULT NULL,
   `justification` text,
   `requested_positions` int DEFAULT NULL,
   `urgency_level` enum('Critical','High','Medium','Low') DEFAULT NULL,
   `target_hiring_date` date DEFAULT NULL,
   `job_role_category` enum('Technical','Non-Technical','Support','Leadership') DEFAULT NULL,
   `hiring_type` enum('Permanent','Contractual','Freelance','Internship') DEFAULT NULL,
   `reporting_manager_id` int DEFAULT NULL,
   `budget_forecast` json DEFAULT NULL,
   `success_metrics` json DEFAULT NULL,
   `priority_weight` decimal(3,2) DEFAULT NULL,
   `market_analysis` text,
   `risk_assessment` json DEFAULT NULL,
   `stakeholder_feedback` json DEFAULT NULL,
   `timeline_milestones` json DEFAULT NULL,
   `created_by` int DEFAULT NULL,
   `status` enum('Draft','Submitted','Approved','Rejected','On Hold','Closed') DEFAULT NULL,
   `workflow_stage` enum('Initial','Job Requirement','Budget Approval','Requisition') DEFAULT NULL,
   `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
   `isDelete` tinyint(1) DEFAULT '0',
   PRIMARY KEY (`business_need_id`),
   KEY `fk_client_business_needs` (`client_id`),
   KEY `fk_department_business_needs` (`department_id`),
   KEY `fk_reporting_manager` (`reporting_manager_id`),
   KEY `fk_created_by_user` (`created_by`),
   CONSTRAINT `fk_client_business_needs` FOREIGN KEY (`client_id`) REFERENCES `Client_Details` (`client_id`),
   CONSTRAINT `fk_created_by_user` FOREIGN KEY (`created_by`) REFERENCES `User` (`user_id`),
   CONSTRAINT `fk_department_business_needs` FOREIGN KEY (`department_id`) REFERENCES `Department` (`department_id`),
   CONSTRAINT `fk_reporting_manager` FOREIGN KEY (`reporting_manager_id`) REFERENCES `Employee` (`employee_id`)
 ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci
 
 CREATE TABLE `Budget_Approval` (
   `budget_id` int NOT NULL AUTO_INCREMENT,
   `business_need_id` int DEFAULT NULL,
   `approved_salary_min` decimal(15,2) DEFAULT NULL,
   `approved_salary_max` decimal(15,2) DEFAULT NULL,
   `approved_benefits` text,
   `relocation_allowance` decimal(10,2) DEFAULT NULL,
   `recruitment_cost_budget` decimal(10,2) DEFAULT NULL,
   `total_budget_approved` decimal(15,2) DEFAULT NULL,
   `fiscal_year` varchar(10) DEFAULT NULL,
   `budget_notes` text,
   `approved_by` int DEFAULT NULL,
   `approval_date` datetime DEFAULT NULL,
   `approval_status` enum('Approved','Rejected','Pending','Revisions Requested') DEFAULT NULL,
   `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
   `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
   PRIMARY KEY (`budget_id`),
   UNIQUE KEY `business_need_id` (`business_need_id`),
   KEY `fk_approved_by_user` (`approved_by`),
   CONSTRAINT `fk_approved_by_user` FOREIGN KEY (`approved_by`) REFERENCES `User` (`user_id`),
   CONSTRAINT `fk_business_needs_budget` FOREIGN KEY (`business_need_id`) REFERENCES `Business_Needs` (`business_need_id`)
 ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci
 
 CREATE TABLE `Audit_Log` (
   `log_id` int NOT NULL AUTO_INCREMENT,
   `user_id` int DEFAULT NULL,
   `action_type` varchar(100) DEFAULT NULL,
   `entity_type` varchar(100) DEFAULT NULL,
   `entity_id` int DEFAULT NULL,
   `old_values` json DEFAULT NULL,
   `new_values` json DEFAULT NULL,
   `ip_address` varchar(45) DEFAULT NULL,
   `user_agent` text,
   `status` enum('SUCCESS','FAILURE') DEFAULT NULL,
   `description` text,
   `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
   PRIMARY KEY (`log_id`),
   KEY `fk_user_audit` (`user_id`),
   CONSTRAINT `fk_user_audit` FOREIGN KEY (`user_id`) REFERENCES `User` (`user_id`)
 ) ENGINE=InnoDB AUTO_INCREMENT=1818 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci
 
 CREATE TABLE `Assessment_Template` (
   `template_id` int NOT NULL AUTO_INCREMENT,
   `template_name` varchar(100) DEFAULT NULL,
   `assessment_type` enum('Technical','Personality','Cognitive','Language','Custom') DEFAULT NULL,
   `skill_category_id` int DEFAULT NULL,
   `difficulty_level` enum('Basic','Intermediate','Advanced','Expert') DEFAULT NULL,
   `duration_minutes` int DEFAULT NULL,
   `passing_criteria` json DEFAULT NULL,
   `questions_bank` json DEFAULT NULL,
   `scoring_algorithm` json DEFAULT NULL,
   `created_by` int DEFAULT NULL,
   `is_active` tinyint(1) DEFAULT '1',
   `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
   `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
   PRIMARY KEY (`template_id`),
   KEY `fk_skill_category_assessment` (`skill_category_id`),
   KEY `fk_user_assessment_template` (`created_by`),
   CONSTRAINT `fk_skill_category_assessment` FOREIGN KEY (`skill_category_id`) REFERENCES `Skill_Category` (`category_id`),
   CONSTRAINT `fk_user_assessment_template` FOREIGN KEY (`created_by`) REFERENCES `User` (`user_id`)
 ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci
 
 CREATE TABLE `Approval_Process` (
   `approval_id` int NOT NULL AUTO_INCREMENT,
   `offer_id` int DEFAULT NULL,
   `approver_id` int DEFAULT NULL,
   `reference_id` int DEFAULT NULL,
   `approval_level` int DEFAULT NULL,
   `reference_type` enum('Interview_Schedule','Offer','Job_Posting','Job_Mapping','Onboarding') NOT NULL,
   `approval_status` enum('Pending','Approved','Rejected') DEFAULT NULL,
   `approval_comments` text,
   `approval_date` datetime DEFAULT CURRENT_TIMESTAMP,
   `reminder_sent` tinyint(1) DEFAULT '0',
   `last_reminder_date` datetime DEFAULT NULL,
   `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
   `updated_at` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
   `payload_data` int DEFAULT NULL,
   PRIMARY KEY (`approval_id`),
   KEY `fk_user_approval_process` (`approver_id`),
   CONSTRAINT `fk_user_approval_process` FOREIGN KEY (`approver_id`) REFERENCES `User` (`user_id`)
 ) ENGINE=InnoDB AUTO_INCREMENT=30 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci
 
 